services:
  web:
    build: .
    container_name: github-readme-streak-stats
    ports:
      - "${PORT:-8080}:80"
    environment:
      - TOKEN=${TOKEN}
      - WHITELIST=${WHITELIST}
      - DISABLE_CACHE=${DISABLE_CACHE}
    restart: unless-stopped
